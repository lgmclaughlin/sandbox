name: filesystem
description: Official MCP filesystem server for safe file access
enabled: false
command: npx
args: ["-y", "@modelcontextprotocol/server-filesystem", "/workspace"]

permissions:
  - filesystem: read
  - filesystem: write
  - network: none

firewall:
  domains:
    - registry.npmjs.org

env: {}
